# Pizarrón de la feria (solo enseñanza)

Esto **no es el producto**. Los agentes se hablan por A2A y ERC-8183. Esta página existe para que la sala vea el oficio sin volarse la cabeza armando una landing.

```
node server.mjs
```

Play 1 = WhatsApp (solo portero). Play 2 = bot sin DNI (solo crítico). Play 3 = alquiler (cajero + escribano). Feria = combo con monedas.

Quote 8183 es real. La moneda que vuela es teatro. DNI aparece en `bag deploy verify`.

En `bag dev`, el oficio de `/demo/work` corre **en local** (0 U, sin Pieverse). El modelo de Pieverse queda para el deploy en el hands-on: esa puerta no existe en la nube.

Recorrer usa el texto que escribas (lo que tire el público).

Un puesto = corrida independiente.

La puerta `/demo/work` solo vive en `bag dev`. En el deploy a BNB **no existe**.
